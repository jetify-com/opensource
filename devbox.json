{
  "packages": [
    "go@latest",
    "golangci-lint@latest"
  ],
  "shell": {
    "init_hook": [
      "echo 'Welcome to devbox!' > /dev/null"
    ],
    "scripts": {
      "build": "find . -type f -name go.mod -maxdepth 4 -execdir go build -v ./... \\;",
      "go-mod-sync": [
        "find . -maxdepth 4 -type f -name go.mod -execdir go mod tidy \\;",
        "go work sync"
      ],
      "lint": "find . -type f -name \"go.mod\" -exec dirname {} \\; | xargs -I {} bash -c \"cd '{}' && golangci-lint run\"",
      "test": "find . -type f -name go.mod -maxdepth 4 -execdir go test -v ./... \\;"
    }
  }
}
